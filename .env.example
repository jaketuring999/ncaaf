# College Football Data API Configuration
# Get your API key from: https://collegefootballdata.com/
CFBD_API_KEY=your_api_key_here

# Optional: Custom endpoint (defaults to official CFBD GraphQL endpoint)
# CFBD_ENDPOINT=https://graphql.collegefootballdata.com/v1/graphql

# Optional: Query timeout in seconds (default: 30)
# QUERY_TIMEOUT=30

# Optional: Maximum retry attempts (default: 3)
# MAX_RETRIES=3

# Optional: Cache TTL in seconds (default: 300)
# CACHE_TTL=300

# Optional: Rate limit requests per minute (default: 100)
# RATE_LIMIT=100

# Optional: Set log level (default: INFO)
# LOG_LEVEL=INFO